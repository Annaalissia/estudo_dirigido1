distancia = float(input("Qual a distancia vai percorrer?: "))
velmed = float(input("Qual a velocidade média?: "))

calculo = distancia / velmed
print("O tempo estimado da viagem é de: ", calculo, "horas")

